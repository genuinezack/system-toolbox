# SCM Breeze for Oh My ZSH

This plugin enables SCM Breeze (assuming you have it installed) on your ZSH shell.

To start using it, add the `scm-breeze` plugin to your `plugins` array in `~/.zshrc`:

```zsh
plugins=(... scm-breeze)
```
